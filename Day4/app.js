$(function () {
  for (var i = 0; i < 10; i++) {
    var button = $('<button>').text('Click me');
    $('.container').append(button)
  }

  for (var j = 0; i < $('button').length; j++) {
    var buttonElement = $('button')[j];
    $()
  }
});
